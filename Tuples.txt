#a tuple is a type of data structure, its a container where we can store values. Multiple pieces of information

coordinates = (4, 5) #tuple with coordinates inside, cannot be modified and deleted
print(coordintes [0])  #follow the proper indexing = prints 4
coordinates = [(4, 5), (6, 7), (8,9)]

